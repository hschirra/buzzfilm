<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.gatech.team55.buzzfilm">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".BuzzLoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                aaa
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".BuzzRegister" />
        <activity android:name=".SearchMovies" />
        <activity android:name=".UserProfile" />
        <activity
            android:name=".SearchResultsActivity"
            android:label="Search Results">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@layout/searchable" />
        </activity>
        <activity android:name=".MovieRating" />
        <activity android:name=".RatingResults" />
        <activity android:name=".GetMovieRecsActivity" />
        <activity android:name=".MovieRecsDisplay" />
        <activity android:name=".AdminMainActivity" />
        <activity android:name=".UserActivity"></activity>
    </application>

</manifest>
